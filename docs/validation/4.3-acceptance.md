# KnowFlow 4.3.0 自动编码验收

## 目标

验证本地 Markdown 自动识别、自动编码、异常项隔离和右键手动修正入口。飞书 proposal 与远端回写仍按原确认边界验收。

## 自动门禁

- Obsidian 插件测试：105/105 通过。
- Obsidian 类型检查：通过。
- 双端构建、版本一致性和发布审计：4.3.0 短编码版本通过。
- SHA256 安装包校验：`fs-TB-Obsidian-4.3.0.zip` `247caf62cdc806b163b1169044d7c27b1a66dc4d114cd90ef00240af2459bb6d`；浏览器包 `68fb384e8a3fa173cfac6a7332120f08ea23cfb675ca41b241e6d6d9a7d78079`。

## 关键行为

- `create` / `modify` / `rename` Markdown 事件经 800ms 防抖后自动生成计划并直接提交安全项。
- 自动事务写入产生的 `modify` / `rename` 事件在短窗口内被抑制，不重复创建自动任务。
- 保护路径、损坏 YAML、非法文档 ID、编码冲突和目标占用只阻断对应文档；同批其他安全文档继续提交。
- 全库命令递归扫描并直接提交安全项，不再打开确认预览。
- 文件树右键改为异常纠错入口：单篇显示短编码编辑框，目录/多选应用安全项；完整编码折叠显示。
- 文件名使用短编码前缀；YAML `编码`、同步事件 `encoding`、索引和唯一性检查继续使用完整编码。

## 真实 Obsidian

- 安装版本：4.3.0 已安装真实 Vault，状态接口返回 `version=4.3.0`。
- 自动触发样例：待用独立测试文档验收。
- 右键手动修正：4.3.0 短编码版本已重装；状态接口返回 4.3.0；实际文件树右键菜单和纠错面板已点击验证（未点击应用，不改真实正文）。
- 真实 Vault 正文：不执行全库自动写入；只使用独立测试文档或既有安全样例。

## 边界

- 自动编码不是远端自动回写；飞书拉取、网页剪藏和回写仍保留 proposal/冲突确认。
- 低置信度只影响标签选择，不阻断安全写入；无法解析或存在冲突的文档保持原样。
