# KnowFlow 4.4.0 标签归类与递归整理验收

## 自动门禁

- 根级 `npm run release:verify`：通过。
- Obsidian 插件测试：108/108 通过。
- 浏览器扩展测试：33/33 通过。
- Obsidian 与浏览器扩展类型检查：通过。
- 版本一致性、发布审计和双端生产构建：通过。
- 安装包 SHA256：
  - `fs-TB-Obsidian-4.4.0.zip`：`4a88675176434ded2e463d527eb19a2a6d356ab27916151ca1aa50a48d77c41b`
  - `KnowFlow-Browser-4.4.0.zip`：`2fd05f7abd9266e2d45bfb070743d7c46010b8e88fac0b15bf0b885db9d4c7a9`
- 构建文件清单：`artifacts/KnowFlow-4.4.0-SHA256SUMS`；安装包清单：`artifacts/KnowFlow-4.4.0-PACKAGES-SHA256SUMS`。

## 关键行为

- 右键菜单使用“设置标签并整理”语义，不再把正常分类变化称作“纠错”。
- 标签候选严格来自协议封闭枚举 `S/X/L/Z/Q/J`。
- 目录默认递归处理子目录中的 Markdown；目录名称只作为范围，不自动重命名。
- 标签变更同步更新 YAML `标签`、完整编码、短编码、文件名、索引和同步事件。
- 文档 ID、日期、序号层级和正文保持不变；冲突、保护路径、损坏 YAML 和并发漂移单篇跳过。

## 真实 Obsidian

- 真实 Vault 已安装 `fs-TB 4.4.0`；状态接口返回 `ok: true`、组件版本 `4.4.0`、协议版本 `1`，`capture-proposal-v1` 能力仍在。
- 已在 `S02 · 剪藏` 目录上实测：右键显示“设置标签并整理此目录（含子目录）…”，面板显示递归范围、预览和完整编码折叠详情；标签下拉包含 `S/X/L/Z/Q/J` 及语义名称。
- 已取消面板，未点击“应用标签变更”；真实 Vault 正文、文件名和 YAML 未执行本次批量标签写入。
- 插件升级仅替换 `main.js`、`manifest.json`、`styles.css`，用户 `data.json` 保留；启动期间只更新了有界活动记录。
