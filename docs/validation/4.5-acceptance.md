# KnowFlow 4.5.0 文件夹自动编码与白名单验收

## 范围

本记录覆盖文件夹结构编码、白名单、右键手动调整、索引保护、双端发布门禁和真实 Obsidian 安装。文档 Markdown 自动编码、标签递归整理与飞书 proposal 继续沿用 4.3/4.4 的验收边界。

## 自动化验证

- [x] `npm test`：根级完整测试通过（本地构建镜像）。
- [x] `npm run build`：共享层、Obsidian 插件和浏览器扩展构建通过。
- [x] `npm run release:verify`：版本一致性、发布审计、安装包和清单生成通过（本地构建镜像）。
- [x] Obsidian 插件专项：`npm test -w @sync/ob-plugin`，120 项通过，包含固定目录结构、文件夹编码、白名单和右键菜单样例。
- [x] Obsidian 类型检查：`npm run typecheck:ob`。
- [x] 独立样例覆盖：名称推断、短/长编码、白名单及子目录、标签冲突、父目录索引路径 rebasing、损坏索引失败关闭。

## 设计不变量

- 文件夹可见名称为 `TAGnn · 标题`；长编码 `YY_MMDD_TAG_NN` 只在 `.feishu-sync/目录编码索引.json` 中保存。
- 序号按标签全 Vault 使用 `01–99`；`Q → Z` 等手动改标签遇到目标序号占用时阻断，不覆盖其他目录。
- 白名单使用相对 Vault 路径，命中目录及子目录；根入口不编码，`🪧导引/`、`3️⃣附件文件/`、隐藏目录、`.obsidian`、`.feishu-sync`、`AGENTS` 永远保护；输入/知识池二级入口固定、三级起编，输出二级起编。
- 不在插件启动时批量改名旧文件夹；文件夹结构标签与 Markdown YAML 标签是两个独立动作。
- 索引写入使用临时文件改名；索引损坏时停止写入，不静默清空。
- 适配 Obsidian `DataAdapter.rename` 不覆盖已有目标的行为：替换前保存旧索引，目标占用时安全替换，失败恢复旧内容，不再循环提示 `Destination file already exists!`。

## 构建产物

构建完成后在此补充同一 commit 生成的 SHA256：

- `artifacts/fs-TB-Obsidian-4.5.0.zip`：`49a89bf5be323aa0cc35ff3be5a1cd833513ab4b30f46c17c664c35444244a59`
- `artifacts/KnowFlow-Browser-4.5.0.zip`：`1da60214ebb8d92466e2007d60496f1ca14ffbec497df5c0464ba0c5e204e420`
- 清单：`artifacts/KnowFlow-4.5.0-SHA256SUMS`、`artifacts/KnowFlow-4.5.0-PACKAGES-SHA256SUMS`

构建清单中的插件主包哈希：`8c3e7db98fcd5fd761ef33f6040cc9f2eaa689f7a9b11358411153887f65c5c3`。

## 真实 Obsidian

- [x] 安装最终 `fs-TB 4.5.0`，保留原有 `data.json`，状态接口返回版本 `4.5.0`、协议 `1` 和 `capture-proposal-v1`。
- [x] 在真实 Vault 的临时目录验证：`0️⃣输入/💡碎片输入_闪念/` 下的三级目录自动变为 `S05 · …`，`2️⃣输出/` 下的二级目录自动变为 `S06 · …`；根级输入、导引和附件保持原名，索引保存 `26_0802_S_05` / `26_0802_S_06`。
- [x] 右键菜单文案与顺序已由 120 项插件专项测试锁定：`剪藏到此文件夹…`、`修改此文档标签…`、`修改此目录标签（含子目录）…`、`修改所选内容标签…`、符合边界时的 `调整此文件夹结构编码…`；不再暴露“检查并纠正”旧入口。
- [x] 白名单、导引、附件、隐藏目录和固定二级入口的保护逻辑由专项测试覆盖；保护目录不进入编码面板。
- [x] 本轮没有改动真实 Vault 现有 Markdown；验收目录已移出到 `/tmp/knowflow-4.5-final-test-*`，索引中的临时记录已清理。

## 发布记录

- Git commit：`a57a74a`（`feat: 固定 Vault 结构并重设计右键菜单`）
- GitHub Release：[v4.5.0](https://github.com/tangzi-kk/knowflow/releases/tag/v4.5.0)
- 真实 Vault 插件路径：`/Users/tangtang/Documents/04_📚_石柯然知识库/.obsidian/plugins/fs-TB/`
