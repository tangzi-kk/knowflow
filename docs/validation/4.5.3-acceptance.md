# KnowFlow 4.5.3 日志导出与编码冲突保护验收

## 修复范围

- 命令面板可搜索并执行“导出同步日志”；Ribbon 入口保留。
- 自动编码在预览阶段阻断同批次内不移动的目标文件和重复目标路径。
- 文件夹自动编码提交前重检目标占用；竞态冲突记录为跳过，不连续弹出失败提示。
- 2026-08-02 15:07:52Z 事务留下的 16 篇提示词文档已从本地恢复快照恢复，内容逐一校验一致。

## 自动化验证

- [x] `npm run test:ob`：Obsidian 插件构建、125 项专项测试、TypeScript 类型检查通过。
- [x] `npm test`：extractor 22、requirements 4、protocol 7、Obsidian 125、browser 33、version 4、audit 4 全部通过。
- [x] `npm run release:package` / `npm run release:manifest`：已生成 4.5.3 双端安装包与 SHA256 清单。
- [x] 真实 Obsidian：已安装并重启 4.5.3；`/status` 返回版本 4.5.3，提示词目录 17 篇 Markdown，Vault 内 `.knowflow-tmp.md` 为 0。

## 关键不变量

- 任何目标路径冲突都在写入前阻断；自动模式只提交安全项。
- 文件夹目标占用属于可预期跳过，不再作为失败循环通知。
- 日志导出只写 `.feishu-sync/` 运行目录，不写入提示词正文或附件。
- 完整编码仍保存在 YAML/索引；文件名和界面只展示短编码。

## 回滚与恢复证据

- 事故快照：`/tmp/knowflow-incident-vmsC07/`。
- 恢复前 Vault 目录快照：`/tmp/knowflow-incident-vmsC07/prompt-dir-before-restore/`。
- 16 个恢复 JSON 与实际恢复文件内容 SHA256 逐一相等。
- Obsidian 安装前插件备份：`/tmp/knowflow-fs-TB-4.5.2-before-4.5.3-xIElNX/`。

## 产物校验

- `fs-TB-Obsidian-4.5.3.zip`：`ac6ef8c2db89bcfa1bf972fea3444c9d79d6d00af2f82fda55889222e5b8a597`
- `KnowFlow-Browser-4.5.3.zip`：`84cc450ac97de65f723954f2fcc8d6211d49c6488798aa719e84fd65f6c64b58`
