# KnowFlow 4.5.5 二级容器入口与启动自动整理验收

## 目标

验证 `0️⃣输入/`、`1️⃣🗃知识池/` 下二级语义目录的容器行为：二级目录自身不编码，启动后自动扫描并整理其直接子文件夹；右键二级容器可直接打开整理预览。

## 自动门禁

- [x] `npm test`：extractor 22、requirements 4、protocol 7、Obsidian 133、browser 33、version 4、audit 4 全部通过。
- [x] `npm run build`：共享层、Obsidian 插件和浏览器扩展生产构建通过。
- [x] `npm run release:package` / `npm run release:manifest`：生成 4.5.5 双端安装包、产物清单和 SHA256。
- [x] `git diff --check`：无空白错误。

## 容器规则回归

- [x] 输入与知识池二级目录被识别为容器，但不会成为编码对象。
- [x] 直接右键二级容器时，预览范围是该容器的直接子文件夹，而不是二级容器的兄弟目录。
- [x] 子文件夹右键仍整理所在容器，并保留当前目标标签下拉修改。
- [x] 启动扫描按名称自然排序、按标签从 `01` 连续编号。
- [x] 容器提交会清除不存在目录对应的失效索引记录。
- [x] 导引、附件、隐藏目录、白名单和固定入口保护边界不变。
- [x] 容器内 Markdown 内容、YAML 和链接不被目录批量改名逻辑写入。

## 真实 Obsidian

- [x] 安装前备份真实 `.obsidian/plugins/fs-TB/`，升级只替换 `main.js`、`manifest.json`、`styles.css`，保留 `data.json`。备份目录：`/tmp/knowflow-fs-TB-4.5.5-before.Gq9LPz/`。
- [x] 重启 Obsidian 后 `/status` 返回 `4.5.5`，协议版本 `1`，插件身份保持正确。
- [x] 启动后索引从 40 条（其中 2 条失效）收敛到 46 条、0 条失效；三个输入二级容器当前均按标签连续编号。启动过程中未发现临时目录残留。
- [ ] 右键 `🎓成长notes_干货`、`💡碎片输入_闪念` 或 `📦项目note_技能` 的真实界面点击验收尚未完成；菜单分支与预览范围已由自动回归测试覆盖。
- [x] `.feishu-sync/目录编码索引.json` 与真实目录一致，Vault 内无残留 `.knowflow-folder-tmp-*`。

## 发布证据

- Git commit：`b437639`（`feat: make semantic folders automatic containers`）
- GitHub Release：[KnowFlow v4.5.5](https://github.com/tangzi-kk/knowflow/releases/tag/v4.5.5)
- Obsidian 包 SHA256：`858c462b3a49c27a16ace9661193aa9bc88c2185e7975dbb5e24bf7f6205e205`
- 浏览器包 SHA256：`dd7042e7b12ec32ae298d09df52200206bc6da6d1ac2dfc3e323a4afffcd1244`
- 真实 Vault 安装备份：`/tmp/knowflow-fs-TB-4.5.5-before.Gq9LPz/`

## 2026-08-04 真实人工全流程验收（部分通过）

### 结论

本次验收在真实飞书文档和真实 Obsidian 4.5.5 上执行到“同步扩展门禁”。样本文档和 Obsidian/lark-cli 前置门禁通过；当前 Chrome 会话没有加载 KnowFlow 浏览器扩展 4.5.5，只有旧的 `Feishu Doc Exporter`，因此没有继续调用接口或用导出器冒充同步扩展，后续同步、幂等、双向、冲突、右键纠错和日志导出均标记为未执行。

### 前置门禁

- [x] 共享盘已挂载，唯一源码仓为 `插件设计/sync-plugin`；验收开始前记录并保留了既有 Git 删除改动，未将其混入本次文档提交。
- [x] `lark-cli` 从 1.0.77 更新到 1.0.82；`lark-cli update --check --json` 报告 binary 与 skills 已一致，用户身份认证可用。
- [x] 真实 Obsidian 插件版本为 4.5.5；重启 Obsidian 后 `/status` 返回 `lark-cli version 1.0.82`、`larkReady: true`。
- [x] `/status` 不带 `X-Sync-Token` 返回 401；带认证请求返回 4.5.5、协议能力和真实 Vault 名称。README 中“无需认证”的旧表述仍应视为文档问题。
- [ ] 当前 Chrome 会话的已加载扩展不是 KnowFlow 4.5.5：页面 DOM 中没有 `feishu-sync-fab` 或 `data-action="feishu-sync"`，本机已安装的相关扩展为旧 `Feishu Doc Exporter`。仓库内 `artifacts/KnowFlow-Browser-4.5.5.zip` 的 manifest 已核对为 4.5.5，但本次没有把未加载的压缩包当作真实扩展结果。

### 真实飞书样本

- [x] 在飞书网页人工新建并保留文档：`【验收保留样本】KnowFlow 同步验收项目 2026-08-04-204302`。
- [x] 样本包含标题层级、正文、无序/有序列表、待办项、表格、代码块、链接和无隐私合成图片，并写入“项目、需求、里程碑、交付、迭代”等确定性信号；图片明确标记为仅用于同步验收。
- [x] 样本内容保存到云端；截图见 [`01-feishu-source-top.png`](assets/4.5.5-2026-08-04/01-feishu-source-top.png) 和 [`02-feishu-source-table-image.png`](assets/4.5.5-2026-08-04/02-feishu-source-table-image.png)。

### 停止点与未执行项

按照计划的“任一门禁失败即停止”规则，本次没有使用 `/fetch`、`/pushback` 或其他接口代替人工扩展流程，也没有修改插件代码来掩盖环境缺口。以下项目保留为待续验：

- [ ] 扩展选择 `0️⃣输入/💡碎片输入_闪念/S03 · 剪藏` 并首次同步。
- [ ] 自动识别 X、完整编码/短编码、稳定文档 ID、图片附件落地和提案收敛。
- [ ] 重复同步幂等、Obsidian 回写、飞书修改后拉取、无变化跳过。
- [ ] 双端冲突 `SYNC_CONFLICT`、人工恢复、右键 `X → Z → X` 和容器递归菜单展示。
- [ ] 日志导出及脱敏核验；本次未生成原始日志，也未将 Token、认证信息或私人正文写入仓库。

截图 [`03-extension-gate-no-knowflow.png`](assets/4.5.5-2026-08-04/03-extension-gate-no-knowflow.png) 仅作为当前页面没有 KnowFlow 同步浮标的门禁证据，不代表同步成功。

## 2026-08-05 格式互通与 YAML 卡片真实复测（通过）

### 结论

在真实飞书文档、真实 Obsidian 4.5.5 和本机 `lark-cli 1.0.82` 上完成了格式样本的实际拉取、正文回写和飞书 XML/Markdown 回读。YAML 字段现在完整落地到本地文件，并在飞书渲染为分组清晰的 `KnowFlow 元数据` 高亮卡片；正文结构、合成图片引用和绑定身份均保留。

### 样本与门禁

- [x] 飞书人工保留样本：`【格式互通验收】KnowFlow 飞书 Obsidian 2026-08-05-0145`。
- [x] Obsidian 实际落地路径：`0️⃣输入/X02.a1 【格式互通验收】KnowFlow 飞书 Obsidian 2026-08-05-0145.md`。
- [x] `/status` 带认证返回 `4.5.5`、协议版本 `1`、`larkReady: true`、`lark-cli 1.0.82`；未带认证仍返回 401。
- [x] 样本覆盖标题、段落、粗斜体、删除线、无序/有序列表、待办、表格、引用、TypeScript 代码块、链接、正文 Callout 和合成图片。

### YAML 与飞书展示

- [x] 本地 YAML 保留完整字段：协议版本、稳定文档 ID、标签、完整编码、短编码、输入、日期、日期索引、关键词、概述、评分、状态、知识库/颜色/操作/块/风险索引及三类关联字段。
- [x] 飞书回读 XML 仅有一个元数据卡片，分为“KnowFlow 元数据”和“系统信息”两段；旧版“文档信息”卡片已被替换。
- [x] 空值统一显示为“未设置”，内部 `feishu_id`、`feishu_doc_id`、`sync_hash`、`sync_time` 不铺到飞书正文。
- [x] 飞书 Markdown 回读仍保留正文可读性，XML 回读确认元数据卡片的字段、完整编码和短编码齐全。

### 真实回写与冲突证据

- [x] 首次回写遇到 `SYNC_CONFLICT`，确认双方修改时不会覆盖内容；该冲突来自前一次旧版本回写结果未知后形成的真实远端差异，不是接口模拟。
- [x] 以当前飞书内容重新建立验收基线后，通过真实 Obsidian 本地服务执行回写，返回 `action: pushed`；本地 `sync_hash` 和 `sync_time` 更新。
- [x] 回读飞书 XML/Markdown 确认新增测试正文仍存在，标题、表格、代码块、Callout 和图片 Block 均未丢失。
- [x] 修复旧回写报错：标题修复不再调用无效的 `--doc-format json`，统一走 XML；`lark-cli 1.0.82` 实测回写成功。
- [x] 修复图片幂等问题：兼容 `internal-api-drive-stream.*` 与 `api3-*-drive.*` 临时图片域名；回写后回读 XML 的新 `src` token 并更新本地 `feishu://` 引用，正文 hash 忽略图片 alt 与文件末尾空行。
- [x] 通过 Obsidian 命令面板真实点击“回写当前文件到飞书”，提示“无变化，已跳过”；随后本地服务重复请求也返回 `action: skipped`，没有重复覆盖或新增文件。

### 结果边界

- [x] 本次真实样本覆盖的 Block 已通过；所有 Block 的官方创建/读取/编辑能力和降级边界以 [`feishu-obsidian-format.md`](../design/feishu-obsidian-format.md) 为准。
- [ ] 尚未对 Bitable、Sheet、Board、OKR、Jira、Iframe 等资源专用或只读 Block 做逐一真实 API 验收；不把能力矩阵当成已完成的真机结果。
- [x] 原始响应、认证信息、Token 和私人正文未写入仓库；验收样本仍保留在用户的飞书和 Obsidian 中。
