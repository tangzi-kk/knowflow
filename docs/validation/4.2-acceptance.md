# KnowFlow 4.2.0 验收记录

日期：2026-08-02

## 自动门禁

- Obsidian 测试：100 项通过。
- 浏览器扩展测试：33 项通过；Obsidian 与扩展类型检查通过。
- 根级发布门禁：需求编辑器、协议、版本一致性、发布审计、双端构建通过。
- 构建产物和安装包 SHA256：`artifacts/KnowFlow-4.2.0-SHA256SUMS`、`artifacts/KnowFlow-4.2.0-PACKAGES-SHA256SUMS` 全部通过。
- 暂存 Vault 烟测：`data.json` 字节级保持不变，插件包只包含 `main.js`、`manifest.json`、`styles.css` 和受保护的设置副本。

## 真实 Obsidian

- `fs-TB` 已安装 4.2.0，Force Reload 后状态接口返回版本 `4.2.0`、协议 `1`。
- 升级保留用户已有设置；新设置 `automaticRecognition` 启用，旧无效开关不再出现，既有 `spaceId` 不被重置。
- 命令面板可找到“自动识别并整理全库文档…”。
- 全库递归预览实际发现 202 项待处理，跳过 0 项；其中 78 项因重复编码阻断，29 项产生低/中置信度识别警告。
- 预览显示目标路径、编码、识别置信度、阻断和警告；长列表可滚动，“取消”和“确认执行可行项”按钮保持可见。
- 本轮只打开并取消预览，没有点击确认；真实 Vault 正文、附件和受保护目录未由本轮自动编码改写。

## 已知边界

- 旧库中的重复编码不会自动合并；它们会在阻断区逐项列出。自动模式可先提交无冲突安全项，冲突项留待单独处理。
- 识别器是确定性规则，不调用 AI；没有足够信号时回退 `S` 并标记低置信度。
- `data.json` 备份放在系统废纸篓的带版本目录中，可恢复；发布包不包含它。
