# KnowFlow 4.5 文件夹自动编码与白名单

## 当前决策

- 协议上游：`tangzi-kk/skr-knowledge` 1.2.1，协议主版本保持为 1。
- 唯一实现：`tangzi-kk/knowflow` 4.5.0；`obsidian-auto-rename` 仅作为事务核心迁移来源。
- 发布状态：4.3.0 已发布；4.5.0 已完成文件夹编码、右键菜单、双端发布门禁和真实 Vault 新建文件夹验收，并已发布 GitHub Release。
- 本机 Obsidian 运行目录：`.obsidian/plugins/fs-TB/`；4.1.1 作为上一版回滚基线保留。

## 2026-08-02 标签归类与递归整理

- 右键菜单按用户意图分成“剪藏到此文件夹”“修改标签”“调整结构编码”三组，不再使用“检查并纠正”等模糊命名。
- 标签候选严格来自 `S/X/L/Z/Q/J`；目录默认递归作用于子目录中的 Markdown，目录名称不作为第二套标签事实。
- 标签变更通过现有恢复点、协调锁、冲突检查、索引重建和同步事件事务，保持文档 ID、日期、序号层级和正文。

## 2026-08-02 文件夹自动编码与白名单

- 文件夹 create/rename 事件自动生成 `TAGnn · 标题`，低置信度回退 `S` 并给出 Notice；插件启动不批量改名旧目录。
- 文件夹长编码独立写入 `.feishu-sync/目录编码索引.json`，按标签全 Vault 使用 `01–99`；目标序号冲突失败关闭。
- 设置页新增相对路径白名单；`🪧导引/`、`3️⃣附件文件/`、隐藏目录和内部目录整树排除；输入/知识池二级入口固定、三级起编，输出二级起编，白名单及子目录递归排除。
- 文件树右键结构编码入口改为“调整此文件夹结构编码…”，与“修改此目录标签（含子目录）…”分离；前者不写 Markdown YAML，并新增“剪藏到此文件夹…”入口。
- 索引损坏或目标路径占用时保留原状态，不静默覆盖。

## 2026-08-02 自动编码与右键纠错

- 从“发现、识别、决策、执行”四个职责重新组织编码流程。
- 新增全库递归自动识别：合法标签优先，标题/关键词/正文规则识别，低置信度回退 `S`。
- 新建、修改、改名事件自动提交安全编码项；插件自身事件会被抑制，异常项提示右键进入纠错面板。
- 文件名与界面默认显示短编码；完整编码仍写入 YAML、同步事件、索引和唯一性判断。
- 全库命令递归扫描并自动提交安全项；本行所述“纠正此文档/检查并纠正”仅为 4.3 旧称，4.5 已改为剪藏、修改标签、调整结构编码三类入口。
- 新安装 `spaceId` 留空，升级保留已有用户值。
- 当前设计入口：`docs/design/4.5-文件夹自动编码与白名单.md`；4.3 文档仅保留文档自动编码的历史边界。

## 2026-07-29 未发布改进

- KF-109：Obsidian 设置页收口为“连接、同步与本地显示、飞书知识库、高级设置”四个单页区段；移除无运行消费者的装饰图片与删除自动登记开关。
- KF-110：右键菜单只处理当前上下文，左侧 Ribbon 只提供任务和状态入口，设置页只保存长期配置；没有新增持久侧栏或第二套写通道。
- 需求编辑器：产品需求改为一次点击直接保存，写入冲突或截图失败时保留草稿并回滚本次附件；记忆候选与知识需求继续独立导出。
- 验证：167/167 自动检查、两端类型检查、完整生产构建、安装包校验和真实 Vault 升级验收通过。

## 子目标状态

| 子目标 | 状态 | 说明 |
| --- | --- | --- |
| 协议快照 | 已完成 | 上游仓库、commit、知识库版本、Schema/协议版本离线锁定 |
| 整理事务 | 已完成 | 预览、确认、全批备份、两阶段换序、逆序恢复、审计与同步事件 |
| 单一 UI | 已完成 | 一套文件树右键、一个 Ribbon、无持久侧栏、设置页窄宽修复 |
| 采集 proposal | 已完成 | fetch/clip 先落地再等待确认；旧服务失败关闭 |
| 发布门禁 | 已完成 | 版本一致性、构建、发布审计、SHA256、暂存 Vault 与真机通信通过 |
| 自动识别与批量编码 | 已完成 | 全库递归自动提交安全项，冲突单篇隔离，低置信度回退，右键手动纠错 |
| 真机安装 | 已完成（4.3.0） | 4.3.0 短编码版本已安装真实 Vault，状态接口返回 4.3.0；未对真实正文执行全库自动写入 |
| 4.4 标签归类 | 已完成（代码/真机预览） | 真实 Obsidian 4.4.0 已验证右键、标签枚举、递归预览和取消路径，未写入真实批量标签 |
| 4.5 文件夹编码 | 已完成并发布 | 文件夹编码与白名单测试、类型检查、最终包安装和真实新建/重命名边界验收通过 |

## 验证记录

- 完整发布基线：167/167 通过；两端 typecheck、build、release audit 与版本一致性通过。
- 通信专项：25/25 通过；真实 localhost 回环：7/7 通过。
- 4.1.1 的构建、SHA256 与真实通信记录作为回滚基线保留。
- 4.2.0 的预览验收记录见 `docs/validation/4.2-acceptance.md`；4.3.0 自动写入记录见 `docs/validation/4.3-acceptance.md`；4.4.0 标签归类记录见 `docs/validation/4.4-acceptance.md`；4.5.0 文件夹编码记录见 `docs/validation/4.5-acceptance.md`。

## 运行记录与边界

- 最近活动使用有界元数据记录，不保存正文、Token 或原始错误；最新一次 `fetch-proposal` 被 `KNOWLEDGE_PLAN_BLOCKED` 安全阻断。
- 飞书 proposal 队列仍为会话级；本地自动编码不依赖待确认任务队列。
- 旧浏览器归档 `Feishu Doc Exporter 0.3.0` 是独立历史产品，不声称能原位升级到 KnowFlow。
