# KnowFlow 需求池

> KnowFlow 需求与整理的唯一入口。以本地文档为准，GitHub 只用于备份、版本记录和同步。

本地编辑入口：[需求编辑器.html](需求编辑器.html)。它只是编辑界面，正式数据仍保存在本文档；需求截图保存在同目录的 `需求附件/`。

## 你平时只需要看这里

有新想法时，直接在“需求收件箱”新增一行。只写一句话也可以，后续再由人或 AI 协助补全、去重和分类。

需要开始实现时，直接说“处理需求池里的 `KF-xxx`”。在这里记录需求不等于自动开始开发。

## 需求收件箱

<!-- 新需求直接加在下面。例如：- [ ] 希望同步前能预览将要修改的文档 -->

- [ ]

## 当前基线

- 当前稳定版：`4.1.0`。
- 双端安装包、SHA256、自动门禁和已记录的真机通信验收已完成。
- 以 `PROGRESS.md`、`VERSION_INDEX.md`、`CHANGELOG.md` 和 `docs/validation/4.1-acceptance.md` 为当前版本证据。
- 旧 `3.x` / `4.0.x` 设计文档中的未勾选项不能直接当作新需求；很多功能已在 `4.1.0` 实现。

## 已确认的功能缺口

### KF-001 删除文件后自动登记

- [ ] 优先级：高
- 问题：README 写成了“删除时自动登记”，但当前实现在删除事件中只记录日志并跳过。
- 期望：删除前安全缓存 `feishu_id → path/title`，删除后进入待确认队列。
- 边界：默认不删除飞书远端，不默认包含子节点，必须显式确认。
- 来源：`packages/ob-plugin/KNOWN_ISSUES.md` 与 `packages/ob-plugin/src/deleteRegistry.ts`。

### KF-002 飞书绑定索引与大库性能

- [ ] 优先级：中
- 问题：按 `feishu_id` 查找文档时，当前仍会遍历并读取 Vault 中的 Markdown。
- 期望：在大 Vault 实测后，如确认成为瓶颈，建立可重建的派生索引。
- 边界：frontmatter 仍是唯一事实源，索引不得变成第二套真相。

### KF-003 `lark-cli` 非阻塞执行

- [ ] 优先级：中
- 问题：当前仍有 `execFileSync` 同步调用，长任务可能阻塞 Obsidian。
- 期望：改为异步、可取消、有总超时且保留真实终态的执行方式。

### KF-004 移除个人飞书知识库 ID 默认值

- [ ] 优先级：中
- 问题：新安装的默认设置中仍有具体 `spaceId`。
- 期望：初次使用时默认留空，由用户在设置或向导中选择；升级时保留已有用户值。

## 待你确认的功能方向

这些来自旧设计稿和路线图，目前只是候选，不表示已经批准开发。

### KF-101 飞书知识库批量同步

- [ ] 优先级建议：高
- 支持按知识库或目录批量发现和同步文档，显示进度、失败项和安全重试，不静默覆盖。

### KF-102 同步仪表盘与待处理收件箱

- [ ] 优先级建议：高
- 显示已同步、待同步、冲突、过期、裂链和回写率；飞书新文档进入待确认收件箱。

### KF-103 冲突提示与差异界面

- [ ] 优先级建议：高
- 在现有三方冲突保护上，增加可读差异、暂停原因、人工选择和恢复入口。
- 边界：双方都修改时默认暂停，不恢复旧设计中的自动 `ob-wins`。

### KF-104 后台定时拉取与规则管道

- [ ] 优先级建议：中
- 支持可关闭的定时发现、条件规则和收件箱分流。
- 边界：自动化仍须服从 proposal 与人工确认，不默认自动编码或回写。

### KF-105 条件模板工厂

- [ ] 优先级建议：中
- 支持全局默认、目录绑定、条件模板和 AI 预填。AI 只建议，最终由人确认。

### KF-106 可查看的同步版本历史

- [ ] 优先级建议：中
- 在现有恢复副本上提供版本列表、差异和显式回滚。

### KF-107 浏览器 AI 与悬浮工具栏剩余体验

- [ ] 优先级建议：低
- 候选项：按域名禁用工具栏、AI 低置信度标识、结果区连续追问。
- 说明：旧设计的大部分 P0/P1 项已实现，不重复列入。

### KF-108 长期方向

- [ ] 优先级建议：低 / 长期
- 候选项：知识关联与语义网络、多 Vault、手机端或飞书机器人、去除 `lark-cli` 并直接使用飞书 OpenAPI。

## 2026-07-29 仓库与文档整理结果

- `sync-plugin/` 现在是 KnowFlow 源码、文档、需求、UI 原型和发布证据的唯一仓库入口。
- 原外部 `插件 UI 设计/` 已迁入 `ui-prototype/`，并明确标注为非运行代码。
- 当前设计入口统一为 `docs/design/README.md`。
- 已实施或被替代的 v3 / 4.0 方案、调研、计划和验收记录已迁入 `docs/history/`。
- `SPEC.md` 已收口为当前 `4.1.0` 产品规格；`docs/AI_HANDOFF.md` 不再保留“未发布、未真机验收”的过时说法。
- 旧根目录 HTML 进展报告和旧总览已移到本机废纸篓，可恢复，不进入新仓库。
- `releases/` 的历史真机归档仍保留。`artifacts/` 中 `4.0.2` 产物在远程 Release 可恢复性完成核对前不删除。

## 详细需求模板（可选）

```markdown
### 需求名称

- 类型：新功能 / 功能改进 / 问题修复
- 优先级：高 / 中 / 低 / 未定
- 当前问题：
- 希望效果：
- 补充说明：
```

## 状态说明

- `[ ]`：尚未完成。
- `[x]`：已经完成或关闭。
- 必要时可在需求后标注：`待确认`、`已确认`、`开发中`、`暂不处理`。
