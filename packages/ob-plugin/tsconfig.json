{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "lib",
    "lib": ["ES2020", "DOM"],
    "noEmit": true,
    // CJS 格式但源码用 ESM import（esbuild 处理）
    "module": "ESNext",
    "moduleResolution": "Bundler"
  },
  "include": ["src/**/*.ts"]
}
